import React from "react";
import { NavLink } from "react-bootstrap";


function Header() {
    return(
        <div>ini header
            <NavLink to="home" 
        >Home</NavLink>
        </div>
    )
}

export default Header;